[project]
name = "deepseek-v3"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.0",
    "safetensors",
    "huggingface_hub",
]
